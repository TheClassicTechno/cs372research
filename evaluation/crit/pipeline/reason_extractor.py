from typing import Dict, Any, List
from ..config import CritConfig


def extract_reasons(
    normalized_transcript: Dict[str, Any],
    omega: str,
    config: CritConfig
) -> List[str]:
    return []
