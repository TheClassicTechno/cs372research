# Standard judge profile — only has judge phase.
system_prompts:
  judge:
    - system_contract/system_causal_contract.txt
    - roles/judge.txt

user_prompts:
  judge:
    template: phases/judge_allocation.txt
    sections: [preamble, context, agent_data, task, output_format]
